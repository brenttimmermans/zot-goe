---
interface Props {
  title: string;
  description: string;
  date: Date;
  slug: string;
  highlights: string[];
  textPositionIndex?: 0 | 1 | 2 | 3;
}

const {
  title,
  description,
  date,
  slug,
  highlights,
  textPositionIndex = 0,
} = Astro.props;

const textOrderClass = `md:order-${textPositionIndex}`;

const formattedDate = date.toLocaleDateString("en-US", {
  year: "numeric",
  month: "long",
});
---

<a href={`/projects/${slug}`} class="group block">
  <div class="grid grid-cols-1 md:grid-cols-4 gap-2">
    <div class={`bg-surface group-hover:bg-bg rounded p-6 flex flex-col justify-center transition-colors ${textOrderClass}`}>
      <h3 class="text-xl font-bold text-heading mb-2">{title}</h3>
      <p class="text-body text-sm mb-4">{description}</p>
      <p class="text-muted text-xs">{formattedDate}</p>
    </div>
    {highlights.map((src, index) => (
      <div class={`overflow-hidden rounded md:order-${index + 1}`}>
        <img
          src={src}
          alt=""
          loading="lazy"
          decoding="async"
          class="w-full h-full object-cover aspect-square"
        />
      </div>
    ))}
  </div>
</a>
